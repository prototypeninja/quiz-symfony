{% extends 'base.html.twig' %}
{% block title %}Admin-Add{% endblock %}
{% block body %}

    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Enregistrer</button>
        <a href="{{path ('admin')}}" class="btn btn-primary mb-3  float-right">Retour</a>
        {{ form_end(form) }}
        <div class="row">
            <h2>Liste des reponses de la question de question </h2>
        {% for reponses in questions.reponsetabs %}

                <div class="alert alert-secondary mt-3 col-md-12" role="alert">
                    {{reponses.reponses}}
                    <a href="{{path ('addQuestion')}}" class="btn btn-primary   float-right ">Modifier</a>
                </div>

        {% endfor %}
        </div>
    </div>

{% endblock %}